# OpenAI API
OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# Optional overrides
WHISPER_MODEL="whisper-1"
LLM_MODEL="gpt-4o-mini"

# Match notebook default (max_tokens for chat completions)
LLM_MAX_OUTPUT_TOKENS="5000"
MAX_AUDIO_SIZE_MB="25"
DEBUG="false"

# Optional: override the default system prompt (multiline OK in .env)
# SYSTEM_PROMPT="..."